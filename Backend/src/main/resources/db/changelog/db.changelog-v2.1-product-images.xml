<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.17.xsd">

    <changeSet id="1" author="modoria">
        <addColumn tableName="products">
            <column name="image_folder" type="VARCHAR(255)"/>
        </addColumn>
    </changeSet>

    <changeSet id="2" author="modoria">
        <createTable tableName="product_images">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="product_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="image_path" type="VARCHAR(1000)">
                <constraints nullable="false"/>
            </column>
            <column name="is_primary" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint baseTableName="product_images"
                                 baseColumnNames="product_id"
                                 constraintName="fk_product_images_product"
                                 referencedTableName="products"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>

        <createIndex tableName="product_images" indexName="idx_product_images_product_id">
            <column name="product_id"/>
        </createIndex>
    </changeSet>

    <changeSet id="3" author="modoria">
        <sql>
            INSERT INTO product_images (product_id, image_path, is_primary)
            SELECT p.id, p.image_path, true
            FROM products p
            WHERE p.image_path IS NOT NULL
              AND NOT EXISTS (
                  SELECT 1
                  FROM product_images pi
                  WHERE pi.product_id = p.id
              );
        </sql>
    </changeSet>

</databaseChangeLog>
