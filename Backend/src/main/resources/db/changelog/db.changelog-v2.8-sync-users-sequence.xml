<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.17.xsd">

    <changeSet id="v2.8-1-sync-users-id-sequence" author="modoria" dbms="postgresql">
        <comment>Keep users_id_seq aligned with existing rows to prevent duplicate users primary keys on register.</comment>
        <sql>
            SELECT setval(
                pg_get_serial_sequence('users', 'id'),
                COALESCE((SELECT MAX(id) FROM users), 0) + 1,
                false
            );
        </sql>
    </changeSet>

</databaseChangeLog>
